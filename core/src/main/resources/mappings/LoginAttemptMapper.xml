<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="liga.medical.medicalmonitoring.core.mapping.LoginAttemptMapper">

    <select id="save" parameterType="liga.medical.medicalmonitoring.core.model.LoginAttempt">
        INSERT INTO login_attempt (dttm, username, comment)
        VALUES (#{dttm}, #{username}, #{comment})
    </select>
</mapper>
